export default function CardProjectList({ user: { username, name, email } }) {
    const usernameConcat = `(${username})`;
  
    return (
      <>
        <div className="Portfolio">
        </div>
      </>
    );
  }